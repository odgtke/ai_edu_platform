# 智慧教育平台渐进式优化实施计划（方案A）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在不改变现有架构的前提下，通过引入Redis缓存、优化推荐算法、提升前端体验，实现系统性能提升50%+

**Architecture:** 采用渐进式优化策略，保持现有Spring Boot + Vue3架构，引入Redis作为缓存层，优化热点数据访问；改进推荐算法权重机制；前端增加骨架屏和懒加载

**Tech Stack:** Spring Boot 2.7.10, Redis, Vue3, Element Plus, MyBatis Plus

---

## 优化模块总览

| 模块 | 优化目标 | 预期收益 | 工作量 |
|------|----------|----------|--------|
| Redis缓存层 | 热点数据缓存 | 响应时间减少60% | 2天 |
| 推荐算法优化 | 动态权重调整 | 推荐准确率提升20% | 2天 |
| 前端体验优化 | 骨架屏/懒加载 | 首屏加载提升40% | 1天 |
| 服务监控 | 基础监控指标 | 问题定位效率提升 | 1天 |

---

## 阶段一：Redis缓存层建设（第1-2天）

### Task 1: 完善Redis缓存配置

**Files:**
- Modify: `backend/src/main/java/com/chinaunicom/edu/config/CacheConfig.java`
- Modify: `backend/src/main/java/com/chinaunicom/edu/config/RedisConfig.java`
- Create: `backend/src/main/java/com/chinaunicom/edu/service/CacheService.java`
- Create: `backend/src/test/java/com/chinaunicom/edu/service/CacheServiceTest.java`

**背景:** 当前CacheConfig仅配置了内存缓存，需要升级为Redis缓存支持更多缓存场景

- [ ] **Step 1: 更新CacheConfig使用RedisCacheManager**

修改 `backend/src/main/java/com/chinaunicom/edu/config/CacheConfig.java`:

```java
package com.chinaunicom.edu.config;

import org.springframework.cache.CacheManager;
import org.springframework.cache.annotation.EnableCaching;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.data.redis.cache.RedisCacheConfiguration;
import org.springframework.data.redis.cache.RedisCacheManager;
import org.springframework.data.redis.connection.RedisConnectionFactory;
import org.springframework.data.redis.serializer.GenericJackson2JsonRedisSerializer;
import org.springframework.data.redis.serializer.RedisSerializationContext;
import org.springframework.data.redis.serializer.StringRedisSerializer;

import java.time.Duration;

@Configuration
@EnableCaching
public class CacheConfig {

    @Bean
    public CacheManager cacheManager(RedisConnectionFactory factory) {
        // 配置序列化
        RedisCacheConfiguration config = RedisCacheConfiguration.defaultCacheConfig()
                .entryTtl(Duration.ofHours(1))  // 默认1小时过期
                .serializeKeysWith(RedisSerializationContext.SerializationPair.fromSerializer(new StringRedisSerializer()))
                .serializeValuesWith(RedisSerializationContext.SerializationPair.fromSerializer(new GenericJackson2JsonRedisSerializer()))
                .disableCachingNullValues();

        return RedisCacheManager.builder(factory)
                .cacheDefaults(config)
                .withCacheConfiguration("users", RedisCacheConfiguration.defaultCacheConfig().entryTtl(Duration.ofMinutes(30)))
                .withCacheConfiguration("courses", RedisCacheConfiguration.defaultCacheConfig().entryTtl(Duration.ofHours(2)))
                .withCacheConfiguration("recommendations", RedisCacheConfiguration.defaultCacheConfig().entryTtl(Duration.ofMinutes(15)))
                .withCacheConfiguration("resources", RedisCacheConfiguration.defaultCacheConfig().entryTtl(Duration.ofHours(1)))
                .build();
    }
}
```

- [ ] **Step 2: 创建CacheService统一缓存操作**

创建 `backend/src/main/java/com/chinaunicom/edu/service/CacheService.java`:

```java
package com.chinaunicom.edu.service;

import org.springframework.data.redis.core.RedisTemplate;
import org.springframework.stereotype.Service;

import java.util.concurrent.TimeUnit;

@Service
public class CacheService {

    private final RedisTemplate<String, Object> redisTemplate;

    public CacheService(RedisTemplate<String, Object> redisTemplate) {
        this.redisTemplate = redisTemplate;
    }

    /**
     * 设置缓存
     */
    public void set(String key, Object value, long timeout, TimeUnit unit) {
        redisTemplate.opsForValue().set(key, value, timeout, unit);
    }

    /**
     * 获取缓存
     */
    public Object get(String key) {
        return redisTemplate.opsForValue().get(key);
    }

    /**
     * 删除缓存
     */
    public void delete(String key) {
        redisTemplate.delete(key);
    }

    /**
     * 判断key是否存在
     */
    public boolean hasKey(String key) {
        return Boolean.TRUE.equals(redisTemplate.hasKey(key));
    }

    /**
     * 设置过期时间
     */
    public boolean expire(String key, long timeout, TimeUnit unit) {
        return Boolean.TRUE.equals(redisTemplate.expire(key, timeout, unit));
    }

    /**
     * 自增
     */
    public Long increment(String key, long delta) {
        return redisTemplate.opsForValue().increment(key, delta);
    }
}
```

- [ ] **Step 3: 编写CacheService单元测试**

创建 `backend/src/test/java/com/chinaunicom/edu/service/CacheServiceTest.java`:

```java
package com.chinaunicom.edu.service;

import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;

import java.util.concurrent.TimeUnit;

import static org.junit.jupiter.api.Assertions.*;

@SpringBootTest
public class CacheServiceTest {

    @Autowired
    private CacheService cacheService;

    @Test
    public void testSetAndGet() {
        String key = "test:key";
        String value = "testValue";
        
        cacheService.set(key, value, 5, TimeUnit.MINUTES);
        Object cached = cacheService.get(key);
        
        assertEquals(value, cached);
    }

    @Test
    public void testDelete() {
        String key = "test:delete";
        cacheService.set(key, "value", 5, TimeUnit.MINUTES);
        
        cacheService.delete(key);
        
        assertNull(cacheService.get(key));
    }

    @Test
    public void testHasKey() {
        String key = "test:exists";
        cacheService.set(key, "value", 5, TimeUnit.MINUTES);
        
        assertTrue(cacheService.hasKey(key));
        assertFalse(cacheService.hasKey("test:notexists"));
    }
}
```

- [ ] **Step 4: 运行测试验证**

```bash
cd c:\Users\Administrator\Desktop\物联网项目\AI_code_2026\backend
mvn test -Dtest=CacheServiceTest
```

Expected: 所有测试通过

- [ ] **Step 5: 提交代码**

```bash
git add backend/src/main/java/com/chinaunicom/edu/config/CacheConfig.java
git add backend/src/main/java/com/chinaunicom/edu/service/CacheService.java
git add backend/src/test/java/com/chinaunicom/edu/service/CacheServiceTest.java
git commit -m "feat: 完善Redis缓存配置，添加CacheService统一缓存操作"
```

---

### Task 2: 为用户查询添加缓存

**Files:**
- Modify: `backend/src/main/java/com/chinaunicom/edu/service/impl/UserServiceImpl.java`
- Modify: `backend/src/main/java/com/chinaunicom/edu/controller/UserController.java`

**背景:** 用户查询是高频操作，添加缓存可显著减少数据库压力

- [ ] **Step 1: 在UserServiceImpl添加缓存注解**

修改 `backend/src/main/java/com/chinaunicom/edu/service/impl/UserServiceImpl.java`，添加以下方法：

```java
import org.springframework.cache.annotation.Cacheable;
import org.springframework.cache.annotation.CacheEvict;
import org.springframework.cache.annotation.CachePut;

@Service
public class UserServiceImpl extends ServiceImpl<UserMapper, User> implements UserService {

    /**
     * 根据ID获取用户（带缓存）
     */
    @Override
    @Cacheable(value = "users", key = "#id")
    public User getById(Long id) {
        return super.getById(id);
    }

    /**
     * 更新用户（更新缓存）
     */
    @Override
    @CachePut(value = "users", key = "#user.userId")
    public boolean updateById(User user) {
        return super.updateById(user);
    }

    /**
     * 删除用户（清除缓存）
     */
    @Override
    @CacheEvict(value = "users", key = "#id")
    public boolean removeById(Long id) {
        return super.removeById(id);
    }
}
```

- [ ] **Step 2: 验证缓存生效**

启动应用后，连续访问同一用户两次：

```bash
# 第一次访问（从数据库）
curl http://localhost:8081/users/1

# 第二次访问（应从缓存）
curl http://localhost:8081/users/1
```

检查日志，第二次不应出现SQL查询

- [ ] **Step 3: 提交代码**

```bash
git add backend/src/main/java/com/chinaunicom/edu/service/impl/UserServiceImpl.java
git commit -m "feat: 为用户查询添加Redis缓存支持"
```

---

### Task 3: 为推荐结果添加缓存

**Files:**
- Modify: `backend/src/main/java/com/chinaunicom/edu/service/impl/RecommendationServiceImpl.java`

**背景:** 推荐计算较耗时，缓存可显著提升响应速度

- [ ] **Step 1: 修改generateRecommendations方法添加缓存**

修改 `backend/src/main/java/com/chinaunicom/edu/service/impl/RecommendationServiceImpl.java`:

```java
import org.springframework.cache.annotation.Cacheable;

@Override
@Cacheable(value = "recommendations", key = "#userId + ':' + #limit")
public List<Map<String, Object>> generateRecommendations(Long userId, Integer limit) {
    try {
        logger.info("Generating personalized recommendations for user: {}", userId);
        
        // 使用混合推荐算法
        List<Map<String, Object>> recommendations = generateHybridRecommendations(userId, limit);
        
        // 保存推荐结果到数据库
        saveRecommendations(userId, recommendations);
        
        return recommendations;
    } catch (Exception e) {
        logger.error("Error generating recommendations for user {}: {}", userId, e.getMessage(), e);
        return new ArrayList<>();
    }
}
```

- [ ] **Step 2: 添加缓存清除方法**

在同一类中添加：

```java
import org.springframework.cache.annotation.CacheEvict;

/**
 * 清除用户推荐缓存
 */
@CacheEvict(value = "recommendations", key = "#userId + ':*'")
public void clearUserRecommendationCache(Long userId) {
    logger.info("Cleared recommendation cache for user: {}", userId);
}

/**
 * 清除所有推荐缓存
 */
@CacheEvict(value = "recommendations", allEntries = true)
public void clearAllRecommendationCache() {
    logger.info("Cleared all recommendation cache");
}
```

- [ ] **Step 3: 提交代码**

```bash
git add backend/src/main/java/com/chinaunicom/edu/service/impl/RecommendationServiceImpl.java
git commit -m "feat: 为推荐结果添加Redis缓存，减少重复计算"
```

---

## 阶段二：推荐算法优化（第3-4天）

### Task 4: 实现动态权重调整机制

**Files:**
- Create: `backend/src/main/java/com/chinaunicom/edu/service/RecommendationWeightService.java`
- Create: `backend/src/main/java/com/chinaunicom/edu/service/impl/RecommendationWeightServiceImpl.java`
- Modify: `backend/src/main/java/com/chinaunicom/edu/service/impl/RecommendationServiceImpl.java`

**背景:** 当前推荐权重固定，应根据用户类型和行为动态调整

- [ ] **Step 1: 创建权重配置服务接口**

创建 `backend/src/main/java/com/chinaunicom/edu/service/RecommendationWeightService.java`:

```java
package com.chinaunicom.edu.service;

import java.util.Map;

/**
 * 推荐权重配置服务
 */
public interface RecommendationWeightService {

    /**
     * 获取用户类型的推荐权重
     * @param userType 用户类型：1-学生 2-教师 3-管理员
     * @return 权重配置Map：content, collaborative, trending, knowledge
     */
    Map<String, Double> getWeightsByUserType(Integer userType);

    /**
     * 获取新用户的冷启动权重
     * @return 权重配置Map
     */
    Map<String, Double> getColdStartWeights();

    /**
     * 根据用户活跃度获取权重
     * @param activityScore 活跃度分数(0-100)
     * @return 权重配置Map
     */
    Map<String, Double> getWeightsByActivity(Integer activityScore);
}
```

- [ ] **Step 2: 实现权重配置服务**

创建 `backend/src/main/java/com/chinaunicom/edu/service/impl/RecommendationWeightServiceImpl.java`:

```java
package com.chinaunicom.edu.service.impl;

import com.chinaunicom.edu.service.RecommendationWeightService;
import org.springframework.stereotype.Service;

import java.util.HashMap;
import java.util.Map;

@Service
public class RecommendationWeightServiceImpl implements RecommendationWeightService {

    /**
     * 学生权重：更注重内容推荐和知识推荐
     */
    private static final Map<String, Double> STUDENT_WEIGHTS = new HashMap<>() {{
        put("content", 0.35);
        put("collaborative", 0.30);
        put("trending", 0.20);
        put("knowledge", 0.15);
    }};

    /**
     * 教师权重：更注重热门和协同过滤
     */
    private static final Map<String, Double> TEACHER_WEIGHTS = new HashMap<>() {{
        put("content", 0.25);
        put("collaborative", 0.35);
        put("trending", 0.25);
        put("knowledge", 0.15);
    }};

    /**
     * 管理员权重：均衡分布
     */
    private static final Map<String, Double> ADMIN_WEIGHTS = new HashMap<>() {{
        put("content", 0.25);
        put("collaborative", 0.25);
        put("trending", 0.25);
        put("knowledge", 0.25);
    }};

    /**
     * 冷启动权重：更依赖热门和内容推荐
     */
    private static final Map<String, Double> COLD_START_WEIGHTS = new HashMap<>() {{
        put("content", 0.40);
        put("collaborative", 0.10);
        put("trending", 0.40);
        put("knowledge", 0.10);
    }};

    @Override
    public Map<String, Double> getWeightsByUserType(Integer userType) {
        switch (userType) {
            case 1: // 学生
                return new HashMap<>(STUDENT_WEIGHTS);
            case 2: // 教师
                return new HashMap<>(TEACHER_WEIGHTS);
            case 3: // 管理员
                return new HashMap<>(ADMIN_WEIGHTS);
            default:
                return new HashMap<>(STUDENT_WEIGHTS);
        }
    }

    @Override
    public Map<String, Double> getColdStartWeights() {
        return new HashMap<>(COLD_START_WEIGHTS);
    }

    @Override
    public Map<String, Double> getWeightsByActivity(Integer activityScore) {
        // 活跃度低时，增加热门推荐权重
        // 活跃度高时，增加协同过滤权重
        Map<String, Double> weights = new HashMap<>();
        
        if (activityScore < 30) {
            // 低活跃度：热门40% + 内容35% + 协同15% + 知识10%
            weights.put("trending", 0.40);
            weights.put("content", 0.35);
            weights.put("collaborative", 0.15);
            weights.put("knowledge", 0.10);
        } else if (activityScore < 70) {
            // 中活跃度：协同35% + 内容30% + 热门20% + 知识15%
            weights.put("collaborative", 0.35);
            weights.put("content", 0.30);
            weights.put("trending", 0.20);
            weights.put("knowledge", 0.15);
        } else {
            // 高活跃度：协同45% + 内容25% + 知识20% + 热门10%
            weights.put("collaborative", 0.45);
            weights.put("content", 0.25);
            weights.put("knowledge", 0.20);
            weights.put("trending", 0.10);
        }
        
        return weights;
    }
}
```

- [ ] **Step 3: 修改RecommendationServiceImpl使用动态权重**

修改 `backend/src/main/java/com/chinaunicom/edu/service/impl/RecommendationServiceImpl.java`:

```java
// 注入权重服务
private final RecommendationWeightService weightService;

public RecommendationServiceImpl(..., RecommendationWeightService weightService) {
    ...
    this.weightService = weightService;
}

// 修改generateHybridRecommendations方法
private List<Map<String, Object>> generateHybridRecommendations(Long userId, Integer limit) {
    Map<Long, Map<String, Object>> courseScores = new HashMap<>();
    
    // 获取用户类型和活跃度，确定权重
    User user = getUserById(userId);
    Map<String, Double> weights;
    
    if (isColdStartUser(userId)) {
        weights = weightService.getColdStartWeights();
    } else {
        Integer activityScore = calculateActivityScore(userId);
        weights = weightService.getWeightsByActivity(activityScore);
    }
    
    // 使用动态权重进行推荐
    List<Map<String, Object>> contentBased = getContentBasedRecommendations(userId, limit);
    addToCourseScores(courseScores, contentBased, weights.get("content"));
    
    List<Map<String, Object>> collaborative = getCollaborativeRecommendations(userId, limit);
    addToCourseScores(courseScores, collaborative, weights.get("collaborative"));
    
    List<Map<String, Object>> trending = getTrendingCourses(limit);
    addToCourseScores(courseScores, trending, weights.get("trending"));
    
    List<Map<String, Object>> knowledgeBased = getKnowledgeBasedRecommendations(userId, limit);
    addToCourseScores(courseScores, knowledgeBased, weights.get("knowledge"));
    
    // ... 后续排序逻辑保持不变
}

/**
 * 判断是否为冷启动用户（行为数据少于5条）
 */
private boolean isColdStartUser(Long userId) {
    Long behaviorCount = userBehaviorMapper.selectCount(
        new QueryWrapper<UserBehavior>().eq("user_id", userId)
    );
    return behaviorCount < 5;
}

/**
 * 计算用户活跃度分数(0-100)
 */
private Integer calculateActivityScore(Long userId) {
    // 基于最近30天的行为数据计算活跃度
    LocalDateTime thirtyDaysAgo = LocalDateTime.now().minusDays(30);
    Long recentBehaviorCount = userBehaviorMapper.selectCount(
        new QueryWrapper<UserBehavior>()
            .eq("user_id", userId)
            .ge("created_time", thirtyDaysAgo)
    );
    
    // 活跃度计算：行为数 * 10，最高100
    return Math.min(100, recentBehaviorCount.intValue() * 10);
}
```

- [ ] **Step 4: 提交代码**

```bash
git add backend/src/main/java/com/chinaunicom/edu/service/RecommendationWeightService.java
git add backend/src/main/java/com/chinaunicom/edu/service/impl/RecommendationWeightServiceImpl.java
git add backend/src/main/java/com/chinaunicom/edu/service/impl/RecommendationServiceImpl.java
git commit -m "feat: 实现推荐算法动态权重调整机制，支持冷启动用户"
```

---

## 阶段三：前端体验优化（第5天）

### Task 5: 添加骨架屏组件

**Files:**
- Create: `frontend/src/components/SkeletonScreen.vue`
- Modify: `frontend/src/views/Courses.vue`
- Modify: `frontend/src/views/Users.vue`

**背景:** 页面加载时显示骨架屏可提升 perceived performance

- [ ] **Step 1: 创建骨架屏组件**

创建 `frontend/src/components/SkeletonScreen.vue`:

```vue
<template>
  <div class="skeleton-screen">
    <el-skeleton :rows="rows" animated v-if="type === 'list'" />
    <el-skeleton :rows="rows" animated v-else-if="type === 'card'">
      <template #template>
        <div style="display: flex; gap: 20px;">
          <el-skeleton-item variant="image" style="width: 240px; height: 160px;" />
          <div style="flex: 1;">
            <el-skeleton-item variant="h3" style="width: 50%;" />
            <el-skeleton-item variant="text" style="margin-top: 12px;" />
            <el-skeleton-item variant="text" style="width: 80%; margin-top: 12px;" />
          </div>
        </div>
      </template>
    </el-skeleton>
    <el-skeleton :rows="rows" animated v-else-if="type === 'table'">
      <template #template>
        <el-skeleton-item variant="text" style="width: 100%; height: 40px; margin-bottom: 10px;" v-for="i in rows" :key="i" />
      </template>
    </el-skeleton>
  </div>
</template>

<script setup>
defineProps({
  type: {
    type: String,
    default: 'list', // list, card, table
    validator: (value) => ['list', 'card', 'table'].includes(value)
  },
  rows: {
    type: Number,
    default: 3
  }
})
</script>

<style scoped>
.skeleton-screen {
  padding: 20px;
}
</style>
```

- [ ] **Step 2: 在Courses.vue中使用骨架屏**

修改 `frontend/src/views/Courses.vue`，在template中添加：

```vue
<template>
  <div class="courses-page">
    <!-- 加载状态显示骨架屏 -->
    <SkeletonScreen v-if="loading" type="card" :rows="5" />
    
    <!-- 加载完成后显示内容 -->
    <div v-else>
      <!-- 原有内容 -->
    </div>
  </div>
</template>

<script setup>
import { ref, onMounted } from 'vue'
import SkeletonScreen from '../components/SkeletonScreen.vue'

const loading = ref(true)
const courses = ref([])

onMounted(async () => {
  loading.value = true
  try {
    // 模拟API调用
    await fetchCourses()
  } finally {
    loading.value = false
  }
})
</script>
```

- [ ] **Step 3: 提交代码**

```bash
git add frontend/src/components/SkeletonScreen.vue
git add frontend/src/views/Courses.vue
git add frontend/src/views/Users.vue
git commit -m "feat: 添加骨架屏组件，优化页面加载体验"
```

---

### Task 6: 实现图片懒加载

**Files:**
- Create: `frontend/src/directives/lazyLoad.js`
- Modify: `frontend/src/main.js`
- Modify: `frontend/src/views/Resources.vue`

**背景:** 资源列表图片较多，懒加载可减少首屏请求

- [ ] **Step 1: 创建懒加载指令**

创建 `frontend/src/directives/lazyLoad.js`:

```javascript
// 图片懒加载指令
const lazyLoad = {
  mounted(el, binding) {
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          const img = el
          const src = binding.value
          
          // 设置加载状态
          img.classList.add('lazy-loading')
          
          // 加载图片
          const tempImg = new Image()
          tempImg.onload = () => {
            img.src = src
            img.classList.remove('lazy-loading')
            img.classList.add('lazy-loaded')
          }
          tempImg.onerror = () => {
            img.classList.remove('lazy-loading')
            img.classList.add('lazy-error')
            // 使用默认图片
            img.src = '/default-image.png'
          }
          tempImg.src = src
          
          // 停止观察
          observer.unobserve(el)
        }
      })
    }, {
      rootMargin: '50px 0px', // 提前50px开始加载
      threshold: 0.01
    })
    
    observer.observe(el)
    
    // 保存observer以便卸载时清理
    el._lazyObserver = observer
  },
  
  unmounted(el) {
    if (el._lazyObserver) {
      el._lazyObserver.disconnect()
    }
  }
}

export default lazyLoad
```

- [ ] **Step 2: 注册全局指令**

修改 `frontend/src/main.js`:

```javascript
import { createApp } from 'vue'
import App from './App.vue'
import router from './router'
import lazyLoad from './directives/lazyLoad'

const app = createApp(App)

// 注册懒加载指令
app.directive('lazy', lazyLoad)

app.use(router)
app.mount('#app')
```

- [ ] **Step 3: 在Resources.vue中使用懒加载**

修改 `frontend/src/views/Resources.vue`:

```vue
<template>
  <div class="resources-list">
    <div v-for="resource in resources" :key="resource.resourceId" class="resource-card">
      <img 
        v-lazy="resource.thumbnailUrl" 
        :alt="resource.resourceName"
        class="resource-thumbnail"
      />
      <div class="resource-info">
        <h4>{{ resource.resourceName }}</h4>
      </div>
    </div>
  </div>
</template>

<style scoped>
.resource-thumbnail {
  width: 200px;
  height: 120px;
  object-fit: cover;
  background: #f0f0f0;
  transition: opacity 0.3s;
}

.resource-thumbnail.lazy-loading {
  opacity: 0.5;
}

.resource-thumbnail.lazy-loaded {
  opacity: 1;
}
</style>
```

- [ ] **Step 4: 提交代码**

```bash
git add frontend/src/directives/lazyLoad.js
git add frontend/src/main.js
git add frontend/src/views/Resources.vue
git commit -m "feat: 实现图片懒加载，减少首屏资源请求"
```

---

## 阶段四：服务监控（第6天）

### Task 7: 添加基础监控指标

**Files:**
- Create: `backend/src/main/java/com/chinaunicom/edu/config/ActuatorConfig.java`
- Modify: `backend/pom.xml`

**背景:** 需要监控服务健康状态和基础指标

- [ ] **Step 1: 添加Spring Boot Actuator依赖**

修改 `backend/pom.xml`，在dependencies中添加：

```xml
<!-- Spring Boot Actuator 监控 -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-actuator</artifactId>
</dependency>

<!-- Micrometer核心 -->
<dependency>
    <groupId>io.micrometer</groupId>
    <artifactId>micrometer-core</artifactId>
</dependency>
```

- [ ] **Step 2: 配置Actuator端点**

创建 `backend/src/main/java/com/chinaunicom/edu/config/ActuatorConfig.java`:

```java
package com.chinaunicom.edu.config;

import org.springframework.boot.actuate.health.Health;
import org.springframework.boot.actuate.health.HealthIndicator;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.data.redis.core.RedisTemplate;

import javax.sql.DataSource;
import java.sql.Connection;

@Configuration
public class ActuatorConfig {

    /**
     * 数据库健康检查
     */
    @Bean
    public HealthIndicator databaseHealthIndicator(DataSource dataSource) {
        return () -> {
            try (Connection connection = dataSource.getConnection()) {
                if (connection.isValid(1)) {
                    return Health.up()
                            .withDetail("database", "MySQL")
                            .withDetail("status", "Connected")
                            .build();
                }
            } catch (Exception e) {
                return Health.down()
                        .withDetail("database", "MySQL")
                        .withDetail("error", e.getMessage())
                        .build();
            }
            return Health.down().build();
        };
    }

    /**
     * Redis健康检查
     */
    @Bean
    public HealthIndicator redisHealthIndicator(RedisTemplate<String, Object> redisTemplate) {
        return () -> {
            try {
                redisTemplate.opsForValue().get("health:check");
                return Health.up()
                        .withDetail("redis", "Connected")
                        .build();
            } catch (Exception e) {
                return Health.down()
                        .withDetail("redis", "Disconnected")
                        .withDetail("error", e.getMessage())
                        .build();
            }
        };
    }
}
```

- [ ] **Step 3: 添加application.yml配置**

修改 `backend/src/main/resources/application.yml`，添加：

```yaml
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: always
  metrics:
    export:
      prometheus:
        enabled: true
```

- [ ] **Step 4: 验证监控端点**

启动应用后访问：

```bash
# 健康检查
curl http://localhost:8081/actuator/health

# 指标数据
curl http://localhost:8081/actuator/metrics

# JVM内存
curl http://localhost:8081/actuator/metrics/jvm.memory.used
```

Expected: 返回JSON格式的健康状态和指标数据

- [ ] **Step 5: 提交代码**

```bash
git add backend/pom.xml
git add backend/src/main/java/com/chinaunicom/edu/config/ActuatorConfig.java
git add backend/src/main/resources/application.yml
git commit -m "feat: 添加Spring Boot Actuator监控，支持健康检查和指标收集"
```

---

## 验证清单

### 性能测试

- [ ] 缓存命中率测试：推荐接口第二次访问响应时间 < 100ms
- [ ] 并发测试：50并发用户，平均响应时间 < 500ms
- [ ] 前端加载测试：首屏加载时间 < 2s

### 功能验证

- [ ] 用户查询缓存生效
- [ ] 推荐结果缓存生效
- [ ] 动态权重根据用户类型调整
- [ ] 冷启动用户使用特殊权重
- [ ] 骨架屏正常显示
- [ ] 图片懒加载正常工作
- [ ] 健康检查端点可访问

---

## 回滚方案

如遇问题，按以下顺序回滚：

1. **Redis缓存问题**：
   ```bash
   # 回退CacheConfig.java到内存缓存版本
   git checkout HEAD~3 -- backend/src/main/java/com/chinaunicom/edu/config/CacheConfig.java
   ```

2. **推荐算法问题**：
   ```bash
   # 恢复固定权重
   git checkout HEAD~1 -- backend/src/main/java/com/chinaunicom/edu/service/impl/RecommendationServiceImpl.java
   ```

3. **前端问题**：
   ```bash
   # 移除骨架屏和懒加载
   git checkout HEAD~2 -- frontend/src/views/Courses.vue
   git checkout HEAD~2 -- frontend/src/views/Users.vue
   ```

---

## 后续优化建议

完成方案A后，可考虑：

1. **引入Redis集群**：提升缓存容量和可用性
2. **添加分布式锁**：防止缓存击穿
3. **实现A/B测试框架**：验证推荐算法效果
4. **前端性能监控**：接入Sentry或类似工具

---

**计划完成时间：6个工作日**
**预期收益：系统性能提升50%+，推荐准确率提升20%**
